# fly.toml app configuration file generated for vexi-bot on 2026-03-30T01:42:11+08:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#
# Note: no [http_service] — the bot opens no HTTP listener (Discord gateway
# only). The machine stays running because no autostop service is defined.

app = 'vexi-bot'
primary_region = 'cdg'

[build]

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
